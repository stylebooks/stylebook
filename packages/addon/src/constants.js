const ADDON_ID = 'stylebookPanel';
const PARAM_KEY = 'stylebookAddon';
const PANEL_ID = `${ADDON_ID}/panel`;
const PANEL_TITLE = 'Stylebook';
const MODES = ['single', 'light-dark-toggle'];

[MODES.SINGLE, MODES.LIGHTDARKTOGGLE] = MODES;

export { ADDON_ID, PARAM_KEY, PANEL_ID, PANEL_TITLE, MODES };
