const clearBuild = require('./clearBuild');
const copyFiles = require('./copyFiles');
const formatPackage = require('./formatPackage');

module.exports = {
  clearBuild,
  copyFiles,
  formatPackage,
};
